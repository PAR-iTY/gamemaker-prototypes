// 2019-01-31 18:55:10
#event step correct for rotation
if(phy_rotation>0) phy_rotation-=5;
if(phy_rotation<0) phy_rotation+=5;

#event collision:obj_static collision with obj_static

direction = direction - 180; // Reverse the directional momentum (ie. stops)
speed = 200;

#event collision:obj_agent automatic collision with self

